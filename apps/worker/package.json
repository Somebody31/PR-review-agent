{
  "name": "@pr-review/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"no lint config yet\""
  },
  "dependencies": {
    "@pr-review/agents": "workspace:^",
    "@pr-review/core": "workspace:*",
    "@pr-review/db": "workspace:^",
    "@pr-review/github": "workspace:^",
    "@pr-review/memory": "workspace:^",
    "@pr-review/shared": "workspace:*",
    "bullmq": "^5.41.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
