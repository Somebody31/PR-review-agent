{
  "name": "pr-review-agent",
  "private": true,
  "version": "0.0.1",
  "description": "AI pull request review agent (TypeScript monorepo)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --parallel --filter @pr-review/api --filter @pr-review/worker run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm --filter @pr-review/shared --filter @pr-review/core --filter @pr-review/db --filter @pr-review/github --filter @pr-review/agents run build && pnpm -r run typecheck",
    "db:migrate": "pnpm --filter @pr-review/db db:migrate",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
